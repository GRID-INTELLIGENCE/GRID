program: analytics-data
version: 1.0
model: ministral
timeout_sec: 30
strict_mode: false

allowed_tools:
  - filesystem
  - git
  - python-repl
  - ollama
  - bash

# external_api allowed for RAG and external provider connectivity
frameworks:
  - pandas
  - numpy

workflows:
  - data_ingestion
  - data_cleaning
  - data_visualization

skills:
  - transform.pivot_table
  - context.data_analysis
  - compress.metric_summary

coverage_threshold: 70