# GRID Docker env (local/CI only). Do not commit secrets.
POSTGRES_USER=grid
POSTGRES_PASSWORD=grid_dev_password
POSTGRES_DB=grid_db
MOTHERSHIP_PORT=8080

# Optional: override to seed models or tuning (unused by compose directly)
# OLLAMA_MODELS="nomic-embed-text"
