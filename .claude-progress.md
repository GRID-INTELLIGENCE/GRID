# Session Progress — 2026-03-28

## Completed

### Admission Gate v2.8.0 Bugfix Plan (ALL 8 STEPS DONE)

**Branch**: `main` at `320d188` (synced with `origin/main`)
**Tests**: 1474 passed, 151 skipped, 2 xfailed — **0 regressions**
**Lint**: clean

#### Step 1: ENTITY_BANNERED violation type
- Added `ENTITY_BANNERED = "entity_bannered"` to `ViolationType` enum
- Base penalty = 0 (bannered entities already blocked)
- Gate 0 rejection now uses `ViolationType.ENTITY_BANNERED` instead of `BUDGET_EXCEEDED`

#### Step 2: Public accessors on EntityAttributionEngine
- `banner_threshold` property
- `peek_record(entity_id)` — returns `None` without auto-creating
- `persist_record(record)` — public wrapper for `_fire_persist()`
- `set_persist_hook(hook)` — wire async persistence callback
- `reset_counters()` — clear admitted/rejected/reasons

#### Step 3: Fix double-instantiation (CRITICAL)
- **Root cause**: `main.py` created two separate `EntityAttributionEngine` instances — one inside Starlette's middleware stack, another on `app.state.admission_gate`. REST API endpoints saw stale/zero data.
- **Fix**: Single shared `EntityAttributionEngine` created in `main.py`, injected into middleware via `attribution=` param, stored on `app.state.admission_attribution`
- Request counters (`total_admitted`, `total_rejected`, `rejection_reasons`) moved from middleware to `EntityAttributionEngine` so both middleware and router share them
- Lifespan hydration updated: `app.state.admission_gate` → `app.state.admission_attribution`
- Router updated: `_get_gate()` → `_get_attribution()`, all `gate.attribution.X` → `attr.X`, all private access eliminated

#### Step 4: Fix double entity lookup in get_entity_report
- Uses `peek_record()` first (no auto-create), then `entity_report()` for formatted output

#### Step 5: Reset-aware persistence sync
- Detects `len(memory) < len(db)` after `full_reset`
- Clears DB violations and re-inserts from memory instead of broken positional slice

#### Step 6: Documentation and cosmetic fixes
- `docs/ARCHITECTURE.md` — admission gate section (gates, penalty tiers, persistence, REST API)
- `frontend/src/types/api.ts` — `ComplianceCheckRequest`, `PenaltyApplyRequest`, `PenaltyRevokeRequest`
- `frontend/src/hooks/use-queries.ts` — `refetchIntervalInBackground: false` on `useAdmissionStats`
- Updated docstring to list all 8 gates

#### Step 7: Tests
- `tests/unit/test_admission_gate.py` — 20 new tests covering:
  - ENTITY_BANNERED enum + zero penalty
  - Public accessors (banner_threshold, peek_record, persist_record, set_persist_hook)
  - Shared attribution injection + counter sharing
  - Persistence reset sync after full_reset
  - HMAC sign/verify (6 test cases)
  - resolve_entity (signed, unsigned fallthrough, no-secret raw)
  - Counter reset
- `tests/unit/test_mothership_post_processing.py` — updated to shared attribution pattern

#### Step 8: HMAC-signed X-Entity-Id
- New file: `src/application/mothership/middleware/entity_signing.py` — HMAC-SHA256 sign/verify with 5-minute replay window
- `resolve_entity()` requires valid signature when `entity_signing_secret` is set
- Silent fallthrough on failure — entity resolves to IP instead of claimed ID
- Config: `admission_entity_signing_secret` in `MothershipSettings.security`

### Commits (4, all pushed)
| Hash | Message |
|------|---------|
| `0afb2ac` | `chore(artifacts): remove obsolete .claude-progress.md and .claude-plan.md` |
| `15af367` | `fix(mothership): shared EntityAttributionEngine, HMAC entity signing, reset-aware persistence` |
| `c9fad0a` | `test(mothership): admission gate shared attribution, HMAC signing, reset persistence` |
| `320d188` | `docs(mothership): admission gate architecture, frontend types, refetchIntervalInBackground` |

### Prior Session Work (also on main)
- Admission Gate System (v2.7.0 → v2.8.0)
- P0-A Config Formalization, P0-B Debug Print Cleanup, P0-C Event Bus Import Fix
- P1-A Auth on Enforcement Endpoints, P1-B Frontend Admission Dashboard
- P2-A $schema Post-Generation Hook, P2-B Entity Persistence (SQLite)
- P3 Codebase Audit Fixes (core_state, grounding, confidence, audit-client)
- Test failure fixes (embedding, path traversal, context storage)

---

## Key Files (this session)

| File | What |
|------|------|
| `src/application/mothership/middleware/admission_gate.py` | ENTITY_BANNERED, public accessors, shared attribution, HMAC resolve_entity, request counters on engine |
| `src/application/mothership/middleware/entity_signing.py` | **NEW** — HMAC sign/verify utilities |
| `src/application/mothership/main.py` | Single shared engine, lifespan hydration update |
| `src/application/mothership/routers/admission_enforcement.py` | `_get_attribution()`, public API only, counter delegation |
| `src/application/mothership/repositories/admission.py` | Reset-aware violation sync |
| `src/application/mothership/config/__init__.py` | `admission_entity_signing_secret` field |
| `docs/ARCHITECTURE.md` | Admission gate section |
| `frontend/src/types/api.ts` | 3 POST request interfaces |
| `frontend/src/hooks/use-queries.ts` | `refetchIntervalInBackground` |
| `tests/unit/test_admission_gate.py` | **NEW** — 20 tests |
| `tests/unit/test_mothership_post_processing.py` | Updated to shared attribution pattern |

---

## Remaining Items

### Environmental (not code bugs)
| Test | Root Cause | Status |
|------|-----------|--------|
| `test_session_lifecycle` / `test_conversation_turn_progression` | Ollama ministral-3:3b model not pulled | Wait for model pull |

### Deep-Dive Verification (for next session — ordered by discipline)
1. **Verify** — [x] Manual smoke test: live counters confirmed, `total_rejected: 1`, `ADMISSION_MISSING_STRUCTURE: 1`, shared engine working
2. **Harden** — [x] HMAC integration test: 9/9 passing (valid sig, invalid sig, expired ts, wrong secret, no secret, edge cases)
3. **Harden** — [x] Persistence reset under file-backed SQLite: 5/5 passing (full reset, partial reset, bannered reset, multi-entity, metadata)
4. **Contract** — [x] OpenAPI v2.8.0 spec regenerated, all `/admission/*` endpoints documented, HMAC features included
5. **Sync** — [x] GRID-main submodule updated to `320d188` in CascadeProjects, pushed to remote
6. **Hygiene** — [x] Dependabot triage: no active alerts, key security packages current, no critical vulnerabilities
